---
label: Recordings
hide_body: true
fields:
- name: recordings
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
  - name: url
    type: text
    config:
      required: true
    label: Url
  - name: source
    type: select
    default: []
    config:
      required: false
      options:
      - YT
      - FB
      - mp3
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Source
  config:
    min: 
    max: 
    labelField: 
  label: Recordings
pages:
- src/assets/recordings.json
